<form action="/me/delete/user?_method=DELETE" method="POST" id="deleteUserForm">
    <table class="table table-user-list">
        <thead>
            <tr>
            <th scope="col">tick</th>
            <th scope="col">userId</th>
            <th scope="col">username</th>
            <th scope="col">password</th>
            <th scope="col">Họ và Tên</th>
            </tr>
        </thead>
        <tbody>
            {{#each users}}
            <tr>
            <td><input class="check-purchase" type="checkbox" value="{{this.userId}}" name="userId"></td>        
            <th>{{this.userId}}</th>
            <td>{{this.username}}</td>
            <td>{{this.password}}</td>
            <td>{{this.name}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    <button type="submit" class="btn btn--primary">Xóa người dùng</button>
</form>

<script>
    document.addEventListener("DOMContentLoaded", function() {
        /*const deleteUserBtn = document.querySelector('.btn-primary');
        const deleteUserForm = document.querySelector('#deleteUserForm');
        deleteUserForm.onsubmit = function(e) {
            e.preventDefault();
            
        }*/
    })
</script>